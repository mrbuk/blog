---
platform: linux

image_resource:
  type: docker-image
  source:
    repository: publysher/hugo
    tag: '0.20'

inputs:
- name: blog
- name : gh-pages

outputs:
- name: new-gh-pages

run:
  path: blog/pipeline/tasks/build/task.sh
